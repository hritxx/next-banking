"use server";

export const signIn = async () => {
  try {
    //mutation /Database / Make fetch request to the server
  } catch (error) {
    console.error(error);
  }
};

export const signUp = async (userData: SignUpParams) => {
  try {
    console.log(userData);
  } catch (error) {
    console.error(error);
  }
};
